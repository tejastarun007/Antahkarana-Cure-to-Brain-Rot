'use client';

export function TopBar() {
  return (
    <div className="sb">
      <span className="sb-t" style={{opacity: 0, pointerEvents: 'none'}}>—</span>
      <div className="sb-r" style={{opacity: 0, pointerEvents: 'none'}}>—</div>
    </div>
  );
}
